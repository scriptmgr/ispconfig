# Claude Rules — ispconfig

This project has its own spec. Read before doing any work:

- `AI.md` — THE HOW (implementation conventions, structure, testing)
- `IDEA.md` — THE WHAT (project description, variables, business logic)

These supersede the global `~/.claude/CLAUDE.md` defaults wherever they conflict.
